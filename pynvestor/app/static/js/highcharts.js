// JavaScript source code to plot lines

function stockChart(toElementId, parameters) {
    // create the chart
    Highcharts.stockChart(toElementId, parameters);
}

function chart(toElementId, parameters) {
    // create the chart
    Highcharts.chart(toElementId, parameters)
}
;

